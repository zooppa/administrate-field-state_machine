<%#
# StateMachine Show Partial

This partial renders the current state of the machine

## Local variables:

- `field`:
  An instance of Administrate::Field::StateMachine.
  A wrapper around the attribute pulled from the database.
%>

<%= field %>
